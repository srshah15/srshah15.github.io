// Stub: fetchGradeData
function fetchGradeData() {
    console.log("fetchGradeData() called");
    // Future code will retrieve data from PostgreSQL
}

// Stub: populateGradebook
function populateGradebook(data) {
    console.log("populateGradebook() called with data:", data);
    // Future code will use 'data' to populate the gradebook table
}

// Call the stubs for testing
fetchGradeData();
populateGradebook([]);
